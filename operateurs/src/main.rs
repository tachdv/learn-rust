fn main() {
    
    // -- LES OPERATEURS ARITHMETIQUES -- // 

    let x = 10;
    let y = 10;
    println!("{} + {} = {}", x, y, x + y); // affiche "10 + 10 = 20"
    println!("{} - {} = {}", x, y, x - y); 
    println!("{} * {} = {}", x, y, x * y);
    println!("{} / {} = {}", x, y, x / y);
    println!("{} % {} = {}", x, y, x % y);

    println!("{} + {} = {}", 5, 5, 5 + 5);
    println!("{} + {} = {}", x, y, 20 - 5);

    // 17 / 5 = 3 il reste 2
    // 17 % 5 = 2 <----- le reste 

}
